#!/usr/bin/env bash

quit() {
    # echo "start EOF"
    kill $ngrok_pid 2> /dev/null
    exit 0
}
trap quit EXIT

# start ngrok (in the background)
./ngrok.sh noui > /dev/null &
ngrok_pid=$!
echo "Starting ngrok.sh..."

# run plotter-server
python main.py
